# Port the server runs on
PORT=5000

# MongoDB connection string (your teammate will replace USER/PASSWORD if needed)
MONGO_URI=mongodb://127.0.0.1:27017/ikpace

# JWT secret (replace with your own secret locally)
JWT_SECRET=your_jwt_secret_here
